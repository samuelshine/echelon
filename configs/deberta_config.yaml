# ============================================================
# Training Configuration — DeBERTa Prompt Injection Detector
# ============================================================

model:
  name: "microsoft/deberta-base"
  num_labels: 2
  label_names:
    0: "benign"
    1: "injection"

tokenizer:
  max_length: 512
  padding: "max_length"
  truncation: true

training:
  epochs: 5
  batch_size: 8
  learning_rate: 1.0e-5
  weight_decay: 0.01
  warmup_ratio: 0.1
  gradient_accumulation_steps: 8   # effective batch size = 64
  fp16: false                       # disabled; MPS uses fp32
  seed: 42
  logging_steps: 50

evaluation:
  strategy: "epoch"
  metric_for_best_model: "f1"
  greater_is_better: true

early_stopping:
  enabled: true
  patience: 2

data:
  train_path: "data/processed/train.csv"
  val_path: "data/processed/val.csv"
  test_path: "data/processed/test.csv"
  text_column: "text"
  label_column: "label"

output:
  model_dir: "models/prompt-injection-deberta/best"
  checkpoint_dir: "models/prompt-injection-deberta/checkpoints"
  results_file: "models/prompt-injection-deberta/results.json"
  confusion_matrix_path: "models/prompt-injection-deberta/confusion_matrix.png"
  roc_curve_path: "models/prompt-injection-deberta/roc_curve.png"
